import BaseLayout from '@/Components/Layout/BaseLayout';
import React from 'react';

export default function Terms(): JSX.Element {
	return (
		<BaseLayout pageTitle="Terms Page">
			<div>Terms Page</div>
		</BaseLayout>
	);
}
